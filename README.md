# 📱 Система репликации и логирования QR-кодов

![QR System](https://img.shields.io/badge/Status-Production%20Ready-green)
![FastAPI](https://img.shields.io/badge/FastAPI-0.104.1-009688)
![Vue.js](https://img.shields.io/badge/Vue.js-3.3.8-4FC08D)
![Docker](https://img.shields.io/badge/Docker-Compose-2496ED)

🚀 **Полное веб-приложение для оперативного сканирования QR-кодов, мгновенной перепечатки и ведения учета отсканированных данных.**

---

## 📋 Содержание

- [Основные возможности](#-основные-возможности)
- [Архитектура системы](#-архитектура-системы)
- [Быстрый старт](#-быстрый-старт)
- [Подробная установка](#-подробная-установка)
- [Конфигурация оборудования](#-конфигурация-оборудования)
- [API документация](#-api-документация)
- [Docker развертывание](#-docker-развертывание)
- [Разработка](#-разработка)
- [Требования к оборудованию](#-требования-к-оборудованию)
- [Лицензия](#-лицензия)

## ✨ Основные возможности

### 📱 **Два режима сканирования**
- **Аппаратный сканер S20-B** - подключение по USB/Bluetooth
- **Камера смартфона/веб-камеры** - через браузер (WebRTC)

### 🖨️ **Интеграция с принтерами**
- Поддержка сетевых принтеров (TCP/IP)
- Поддержка USB/Bluetooth принтеров
- Браузерная печать как fallback
- Автоматическая отправка на печать после сканирования

### 📊 **Логирование и отчетность**
- Полное логирование всех операций в MySQL
- Экспорт истории в Excel (.xlsx)
- Фильтрация по датам и статусам

### 🐳 **Простое развертывание**
- Полная Docker-контейнеризация
- Готовая конфигурация Docker Compose
- Простая настройка через переменные окружения

## 🏗️ Архитектура системы

```
qr-replication-system/
├── backend/          # FastAPI приложение
│   ├── app/
│   │   ├── models.py    # SQLAlchemy 2.0 модели
│   │   ├── services/     # Бизнес-логика
│   │   └── routers/     # API endpoints
├── frontend/          # Vue.js 3 приложение
│   ├── src/
│   │   ├── views/       # Страницы приложения
│   │   └── components/  # Переиспользуемые компоненты
├── docker-compose.yml   # Docker Compose конфигурация
└── nginx/            # Конфигурация Nginx
```

---

**Технологический стек:**
- **Backend:** FastAPI, SQLAlchemy 2.0, MySQL
- **Frontend:** Vue.js 3, Vite, PrimeVue
- **Сканирование:** pyzbar (камера), HID-клавиатура (сканер)
- **Печать:** Socket/ZPL для сетевых принтеров, Web Print API для браузера
- **Контейнеризация:** Docker, Docker Compose

## 🚀 Быстрый старт

### 1. Клонирование и настройка
```bash
# Клонируйте репозиторий
git clone <repository-url>
cd qr-replication-system

# Создайте файлы окружения
cp backend/.env.example backend/.env
```

### 2. Запуск через Docker (рекомендуется)
```bash
# Запуск всей системы
docker compose up -d --build

# Проверка статуса
docker compose ps

# Просмотр логов
docker compose logs -f backend
```

### 3. Доступ к приложению
- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:8000
- **API документация:** http://localhost:8000/docs
- **База данных MySQL:** localhost:3306

## 📦 Подробная установка

### Вариант A: Ручная установка

**Backend:**
```bash
cd backend
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или venv\Scripts\activate  # Windows

pip install -r requirements.txt

# Запуск сервера
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**Frontend:**
```bash
cd frontend
npm install
npm run dev
```

### Вариант B: Docker
```bash
docker compose up -d --build

docker compose ps
docker compose logs -f backend
```

### Доступ к приложению:
- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:8000

---

## 🔧 Конфигурация оборудования

### 📷 Сканер S20-B

**Подключение и настройка:**

**USB подключение:**
1. Подключите сканер к USB порту компьютера
2. Сканер автоматически определится как HID-клавиатура
3. Наведите на QR-код - данные поступят в поле ввода

**Bluetooth подключение:**
1. Включите сканер (удерживайте кнопку 3 секунды)
2. На компьютере найдите устройство "S20-B"
3. Подключитесь (пароль: 0000 или 1234)
4. Сканер готов к работе

**Конфигурация сканера (через QR-коды):**

Сканер S20-B настраивается путем сканирования специальных QR-кодов:

| Настройка | QR-код для сканирования |
|-----------|--------------------------|
| Включить Enter | Сканируйте: [ENTER] |
| Выключить Enter | Сканируйте: [NOENTER] |
| Добавить префикс | Сканируйте: [PREFIX]XXX |
| Добавить суффикс | Сканируйте: [SUFFIX]YYY |
| Сброс настроек | Сканируйте: [RESET] |

**Автофокус в приложении:**
- Поле ввода автоматически фокусируется при загрузке
- После сканирования поле очищается и фокусируется снова
- Поддерживается горячая клавиша `Ctrl+K` для ручного фокуса

### 🖨️ Настройка принтеров

1. **Сетевые принтеры (рекомендуется):**
```yaml
# Пример конфигурации для сетевого принтера
Имя: "Производственный принтер"
Тип: "network"
IP: "192.168.1.100"
Порт: 9100
```

2. **Браузерная печать:**
- Используйте встроенную печать браузера
- Настройте макет этикетки в интерфейсе
- Поддерживается предварительный просмотр

3. **Интеграция с QZ Tray (альтернатива):**
```javascript
// Для автоматической печати без диалогов
// Требуется установка QZ Tray на клиентский ПК
qz.websocket.connect().then(() => {
    qz.print(config, data);
});
```

---

## 📡 API документация

### Основные endpoints

**Сканирование:**
```http
POST /api/scans/
Content-Type: application/json

{
    "qr_content": "QR123456789",
    "scan_source": "scanner"
}
```

**Сканирование через изображение:**
```http
POST /api/scans/scan-image/
Content-Type: multipart/form-data
file: [изображение с QR-кодом]
```

**Экспорт в Excel:**
```http
GET /api/scans/export/
?start_date=2024-01-01&end_date=2024-01-31
```

**Управление принтерами:**
```http
GET /api/printers/          # Список принтеров
POST /api/printers/         # Добавить принтер
POST /api/printers/test/{id} # Тест принтера
```

### Примеры использования

**Python:**
```python
import requests

# Сканирование QR-кода
response = requests.post(
    "http://localhost:8000/api/scans/",
    json={"qr_content": "PRODUCT-12345", "scan_source": "scanner"}
)

# Экспорт отчета
response = requests.get(
    "http://localhost:8000/api/scans/export/",
    params={"start_date": "2024-01-01", "end_date": "2024-01-31"}
)
```

**cURL:**
```bash
# Сканирование
curl -X POST http://localhost:8000/api/scans/ \
  -H "Content-Type: application/json" \
  -d '{"qr_content":"TEST123"}'

# Получить историю
curl http://localhost:8000/api/scans/

# Экспорт Excel
curl -o report.xlsx http://localhost:8000/api/scans/export/
```

---

## 🐳 Docker развертывание

### Production конфигурация

1. **Настройка переменных окружения:**
```env
# backend/.env
DATABASE_URL=mysql+pymysql://user:strongpassword@mysql:3306/qr_system
ALLOWED_ORIGINS=["https://ваш-домен.com"]
DEBUG=False
```

2. **Docker Compose для production:**
```yaml
# docker-compose.prod.yml
version: '3.8'

services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: qr_system
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./backup:/backup
    restart: always
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    environment:
      DATABASE_URL: mysql+pymysql://${DB_USER}:${DB_PASSWORD}@mysql:3306/qr_system
    restart: always
    depends_on:
      mysql:
        condition: service_healthy

  frontend:
    build: ./frontend
    restart: always

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./ssl:/etc/nginx/ssl
    restart: always
```

3. **Запуск в production:**
```bash
# Создайте .env файл с секретами
echo "DB_PASSWORD=your_strong_password" > .env
echo "DB_USER=qr_user" >> .env

# Запуск
docker-compose -f docker-compose.prod.yml up -d

# Настройка SSL (опционально)
# См. раздел "Дополнительные улучшения"
```

### Мониторинг и логирование

**Просмотр логов:**
```bash
# Все логи
docker-compose logs

# Логи конкретного сервиса
docker-compose logs backend

# Логи в реальном времени
docker-compose logs -f frontend

# Размеры контейнеров
docker-compose ps --size
```

**Резервное копирование базы данных:**
```bash
# Ручной бэкап
docker exec qr-mysql mysqldump -u user -ppassword qr_system > backup.sql

# Автоматический бэкап (добавьте в crontab)
0 2 * * * docker exec qr-mysql mysqldump -u user -ppassword qr_system | gzip > /backup/qr_system_$(date +\%Y\%m\%d).sql.gz
```

---

## 💻 Разработка

### Структура проекта

**Backend (FastAPI):**
```
backend/app/
├── main.py              # Точка входа
├── models.py           # SQLAlchemy 2.0 модели
├── schemas.py          # Pydantic схемы
├── services/           # Бизнес-логика
│   ├── qr_service.py   # Генерация/декодирование QR
│   ├── print_service.py # Работа с принтерами
│   └── excel_service.py # Генерация Excel
└── routers/            # API роутеры
    ├── scans.py        # Сканирование
    ├── printers.py     # Принтеры
    └── print.py        # Печать
```

**Frontend (Vue 3):**
```
frontend/src/
├── views/              # Страницы приложения
│   ├── ScannerView.vue # Главная страска сканирования
│   ├── HistoryView.vue # История сканирований
│   └── PrintersView.vue # Управление принтерами
├── components/         # Переиспользуемые компоненты
├── router/            # Vue Router конфигурация
└── services/          # API клиенты
```

---

## 🖥️ Требования к оборудованию

- **Сервер:** 2 ядра CPU, 4GB RAM (рекомендуется 4 ядра, 8GB RAM)
- **Хранилище:** 10GB свободного места (SSD рекомендуется)
- **Сетевой принтер:** Zebra, Brother, или совместимый с ZPL
- **Сканер штрих-кодов:** S20-B или аналогичный
- **Браузер:** Современный браузер (Chrome, Firefox, Edge)

